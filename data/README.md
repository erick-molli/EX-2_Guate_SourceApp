# SA_RProject

The `data` folder is divided in the following subfolders:
-The `raw_data` folder contains all the raw data used for data analyses.
-The `processed_data` folder contains all the data that has been processed after running the `SA_data_cleaning` code.
